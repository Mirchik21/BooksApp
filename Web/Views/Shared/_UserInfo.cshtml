<div class="dropdown">
    <div class="user col align-self-end">
        <img src="~/images/avatar.png" id="userDropdown" alt="" data-toggle="dropdown" aria-haspopup="true"
             aria-expanded="false" />
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
            <div class="dropdown-header">
                <label class="font-weight-bold">Email: </label>@User.Identity.Name
            </div>            
            <a asp-action="Logout" asp-controller="Account" class="dropdown-item">Выход</a>
        </div>
    </div>
</div>